#cloud-config

package_update: true
package_upgrade: true

packages:
  - nginx

write_files:
  - path: /var/www/html/index.html
    content: |
      <h1>Hello from Terraform + Cloud-Init (Ubuntu)</h1>

runcmd:
  - systemctl enable nginx
  - systemctl start nginx