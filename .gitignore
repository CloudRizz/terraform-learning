# Terraform working directories
.terraform/

# Terraform dependency lock file (optional, but recommended to be included in version control)
.terraform.lock.hcl

# Terraform state files (VERY IMPORTANT)
*.tfstate
*.tfstate.*
terraform.tfstate
terraform.tfstate.backup

# Crash logs
crash.log
crash.*.log

# CLI config files
.terraformrc
terraform.rc

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Plan output files (if you save plans)
*.tfplan